# Tab: Palette

The Palette tab is an example tab showcasing elements and functionality related to forms, network, and shared styling.

A variant of `palette` is used in the `miskweb new` command to create a new Misk Web tab.

[Check it out here.](/examples/tabs/palette/demo/)
